version: '3.8'
services:
  frontend:
    build:
      context: ./datasci-ui
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - FLASK_API_PATH="http://backend:8080"
    networks:
      - app-network

  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8080:8080"
  #   networks:
  #     - app-network

networks:
  app-network:
    driver: bridge
